bindsym F1 exec echo "key ctrl+c" | dotool && swaymsg '[app_id="io.github.cboxdoerfer.FSearch"] focus' && echo "key ctrl+a" | dotool && echo "key ctrl+v" | dotool

bindsym $meta+F12 exec echo type $(wl-paste) | dotool

bindsym $meta+F10 exec < "/home/blu/.config/sway/bitv-template.txt" dotool

bindsym $meta+Home exec echo type $(date '+%a %Y-%m-%d') | dotool
# bindsym F1 exec dotoold & (echo "key ctrl+c" && swaymsg '[app_id="io.github.cboxdoerfer.FSearch"] focus' && echo "key ctrl+a" && echo "key ctrl+v") | dotoolc

bindcode 196 exec bash -c 'pgrep -x dotoold >/dev/null || dotoold & sleep 0.1; for i in {1..3}; do echo click left; sleep 0.05; done | dotoolc'
